# Consumer ProGuard/R8 rules shipped with cc.linklab:android.
# The SDK has no reflection-based entry points, so no keep rules are required.
# OkHttp and the Install Referrer library ship their own consumer rules.
